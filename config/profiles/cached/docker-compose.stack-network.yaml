# modular：接入 deploy/stack-network，访问 stack 中的 PostgreSQL / Ollama
services:
  centag:
    networks:
      - centag-cached
      - deploy/stack-network

networks:
  deploy/stack-network:
    external: true