# Gateway Profile - 统一接入网关
STACK_MODE=modular
# 默认 OLLAMA_ENABLED=false → 无 stack 依赖，仅启应用容器
# 设 OLLAMA_ENABLED=true 时才会 stack ensure ollama
STACK_DEPS="ollama"
DESCRIPTION="Unified LLM gateway with full database support and multiple backend routing"