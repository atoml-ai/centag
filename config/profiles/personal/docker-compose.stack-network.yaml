# modular + OLLAMA_ENABLED：接入 deploy/stack-network，访问 stack 中的 Ollama
services:
  centag:
    networks:
      - centag-personal
      - deploy/stack-network

networks:
  deploy/stack-network:
    external: true