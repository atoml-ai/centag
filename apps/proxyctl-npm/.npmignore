# Keep the main package tiny: never bundle cross-compiled binaries.
# The offline variant (centag-proxyctl-offline) is built from package.offline.json
# and intentionally includes bin/vendor. This ignore only applies to the main package.
bin/vendor/
node_modules/
*.tgz
