---
description: "Centag 向导测试 — Cursor 交互入口。仅 AskQuestion 收参；业务执行见 docs/harness/skills/centag-wizard-test.md。分层见根目录 AGENT.md。"
globs: []
alwaysApply: false
---

# Centag Wizard Test — Cursor 交互入口

> **本文件只做交互。** 业务正本：`docs/harness/skills/centag-wizard-test.md`  
> 分层原则：根目录 `AGENT.md`

## 硬性规则

1. 固定选项必须调用工具 **`AskQuestion`**（禁止正文列 A/B/C）。
2. 收齐参数并确认后，**加载正本执行**，不在此文件发明步骤。
3. AskQuestion 不可用时才允许极短文字提问。

## 触发词

`向导测试` / `wizard test` / `centag 向导` / `一键测试` / `测试向导` / `开始向导` / `开始测试向导`

## 流程

### 0. 静默探测

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:20060/health
```

非 200 → AskQuestion：手动启动 / agent 自动 / 退出。  
进程名含 `centag-mi` 时默认建议 `minimal`。凭据优先 `config/secrets/.env`。

### 1. AskQuestion — 基础配置

| id | prompt | options |
|----|--------|---------|
| `deploy_type` | 部署类型 | gateway / team / minimal / personal |
| `base_url` | 服务地址 | `http://localhost:20060`；其他（我会输入） |
| `admin_creds` | Admin 凭据 | 从 .env 读取；其他（我会输入） |
| `test_type` | 测试类型 | pipeline / admin / all |

### 2. 若 pipeline|all → AskQuestion — 后端与范围

| id | prompt | options |
|----|--------|---------|
| `backend_source` | 后端来源 | real / mock |
| `backend_id` | 后端 | deepseek / bigmodel / openai / opencode-zen / siliconflow / alibaba-dashscope / ollama-local / 其他 |
| `test_scope` | Pipeline 范围 | 快速 / 标准 |

范围与后端默认模型等**以正本为准**，勿在入口硬编码业务表的第二份副本以外的逻辑。

### 3. AskQuestion — 确认

`start` = 开始测试；`modify` = 返回修改

### 4. 交接正本

导出 `CENTAG_DEPLOY_TYPE`、`TEST_DEPLOY_TYPE`、`TEST_BASE_URL`、`ADMIN_*`、`TEST_*` 后执行：

**`docs/harness/skills/centag-wizard-test.md`**
