---
description: "Centag GitHub Release 发版 — Cursor 交互入口。仅 AskQuestion 收参；业务执行见 docs/harness/skills/centag-release.md。分层见根目录 AGENT.md。"
globs: []
alwaysApply: false
---

# Centag Release — Cursor 交互入口

> **本文件只做交互。** 业务正本：`docs/harness/skills/centag-release.md`  
> 分层原则：根目录 `AGENT.md`

## 硬性规则

1. 固定选项必须调用工具 **`AskQuestion`**（禁止正文列 A/B/C）。
2. 收齐参数并确认后，**加载正本执行**，不在此文件发明步骤。
3. AskQuestion 不可用时才允许极短文字提问。
4. 当前范围仅 **personal + proxyctl**；用户要求 minimal/launcher 时说明「正本标明以后再说」，勿擅自发版。
5. **仅版本分支可发布**（`vX` / `feature/vX` / `release/vX`）：非版本分支则引导切换；禁止建议 `CENTAG_RELEASE_ALLOW_ANY_BRANCH`（除非用户明确要求紧急绕过）。

## 触发词

`发版` / `release` / `打 release` / `发布 github release` / `publish release` / `一键发版` / `centag release`

## 流程

### 0. 静默探测

```bash
git rev-parse --abbrev-ref HEAD
VER="$(node -p "require('./apps/proxyctl-npm/package.json').version" 2>/dev/null || true)"
bash scripts/release/require-release-branch.sh --version "${VER:-0.0.0}" 2>&1 || true
gh auth status 2>&1 | head -20
ls bin/release/*/centag-*.tar.gz 2>/dev/null | head -20
echo "package.json version=${VER}"
```

### 1. AskQuestion — 发版参数

| id | prompt | options |
|----|--------|---------|
| `version` | 版本号（无 v 前缀） | 读自 package.json；其他（我会输入） |
| `path` | 发版路径 | A 已有产物只上传 / B 脚本重建并上传 / C 推 tag 走 CI |
| `draft` | 是否草稿 | 是（推荐）/ 否直接公开 |
| `verify` | 发版后验收 | 本机 install 冒烟 / 仅检查 Release 资产 / 两者都做 |

### 2. 导出环境变量后交接正本

```bash
export CENTAG_RELEASE_VERSION=...      # 无 v
export CENTAG_RELEASE_PATH=A|B|C
export CENTAG_RELEASE_DRAFT=true|false
export CENTAG_RELEASE_REPO=atoml-ai/centag   # 可选
export CENTAG_INSTALL_REF=main               # 可选；未合入 main 时用当前分支
```

然后 **打开并严格执行** `docs/harness/skills/centag-release.md`。
