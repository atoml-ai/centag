# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/centag
/centag-server
/centag-test
/centag-minimal
/minimal
/gateway
/team
/main
# Temporary test files
check_admin_key.go
check_api_key.go
test_regex.go

# Root npm lockfile only（web/package-lock.json 需提交供 Docker npm ci）
/package-lock.json

# Test binary
*.test

# Output — runtime & build artifacts (sources live under web/, config/, cmd/)
var/
!var/.gitkeep
deploy/docker/data/
# dist/ 仅提交发行版源码（minimal/gateway/team）；渠道构建物默认在 ~/.centag/var/packages/
/dist/*.fpk
/dist/minimal/minimal
/dist/gateway/gateway
/dist/minimal/centag*
/dist/gateway/centag*
/dist/personal/centag*
*.tar.gz
*.fpk

# Wails desktop build outputs (if reintroduced locally)
web/build/

# Misplaced build output when scripts run with wrong ROOT
scripts/desktop/
scripts/webui/

# Logs
*.log
logs/

# System Proxy - Runtime generated files
*.old
/tmp/centag-*.log
/tmp/centag-original-settings.txt

# IDE
.vscode/
.idea/
.VSCodeCounter/
.codegraph/
.wps-comate/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Cloud CLI configs
.aliyun/

# Python
__pycache__/

# Config files - all configs are in config/initdata, the root configs/ is for development only
configs/
!configs/*.example

# Environment files
.env.local
.env.*.local
.env
**/.env.middleware

# Secrets directory (contains authentication credentials)
config/secrets/.env
config/secrets/.env.middleware
config/secrets/*.key
config/secrets/*.pem
config/secrets/*.crt
config/secrets/.env.backup.*

# Initdata secrets (local API keys; do not commit)
config/initdata/secrets/*.key
config/initdata/*.key
config/initdata/PPIO.md

# Scripts with sensitive data
scripts/update-api-keys.sh
scripts/update-backends.sh

# 遗留：若本地仍放置 deploy/docker/.env 则勿提交（真源为 config/secrets/.env）
deploy/docker/.env

# Docker override files (auto-generated)
docker-compose.override.yaml

# Web
web/node_modules/
web/dist/
web/.vite/
web/.cache/
web/.env*
web/.env.development
# E3: symlink to centag-pro/web/packs/team during team frontend builds
web/.centag-team-pack

# Docker static files (built from web/)
deploy/docker/static/assets/
deploy/docker/static/index.html
deploy/docker/static/*.js
deploy/docker/static/*.css

# Documentation files (auto-generated)
WEBUI_*.md
CACHE_UI_UPDATE.md
LAYOUT_OPTIMIZATION.md

# Temporary files
*.tmp
*.temp
*.bak
.cache/
# Test outputs
test-results/
coverage/
coverage.out
*.coverprofile

# Pipeline test reports (auto-generated)
docs/test-reports/

# Database files (runtime generated)
*.db
!internal/database/migrations/*.sql

# Developer tools (utility scripts, not part of project)
cmd/tools/

# Debian packages
*.deb
centag-bin
*.pyc

config/profiles/*/.env

# Legacy runtime paths (pre directory-restructure)
/storage/
/bin/
/static/
/certs/
/data/
/packages/
/tmp/
secrets/.env

# Runtime secrets generated under bin/server (also covered by /bin/)
**/jwt.secret
**/admin.password.hash
**/*.password.hash
**/ca.key
**/api_key_storage.secret

# Runtime server data directory (generated at startup)
core/pkg/server/data/

# npm centag-npm build artifacts (cross-compiled binaries, generated during publish)
apps/centag-npm/bin/vendor/
apps/centag-npm/static/
# npm centag-wrap build artifacts (cross-compiled binaries, generated during publish)
apps/wrap-npm/bin/vendor/
apps/wrap-npm/static/

# Build artifacts
node.tar.xz

# AI Assistant build artifacts (if reintroduced locally)
web/ai-assistant/bin/
web/ai-assistant/tui

# node_modules (root level)
node_modules/

# Broken / nested git metadata (deploy/stack is a normal directory, not a submodule)
deploy/stack/.git

generate-protocol-report
test_results.json
protocol-test-report.html
test_results.json
.workbuddy/memory/
.opencode/
