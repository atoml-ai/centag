# 自测记录 — 普通用户 WebUI 能力矩阵

> 版本：v0.2.6 | 分支：`feature/v0.2.6` | 日期：2026-07-20

## 自动化已通过（含 Step 4 / Gate 3）

| 项 | 命令 / 说明 | 结果 |
|----|-------------|------|
| UI capability 单测 | `cd web && npm run test:ui-caps`（capabilities / edition-routes / nav / dashboard-sections） | ✅ |
| 前端 lint | `cd web && npm run lint:ci` | ✅ |
| harness-check | `make harness-check` | ✅ |
| golangci-lint | `make lint` | ✅ 0 issues |
| 本需求相关 Go | `cd core && go test ./pkg/server/ ./pkg/useraccess/ ./internal/edition/ -count=1` | ✅ |
| storage 写拦截 | `TestTeamAdminWriteOnly_*`（team normal 403；personal/minimal/admin 放行） | ✅ |
| UI 浏览器验收（T8） | Playwright MCP；`/tmp/centag_ui_browser_results.json` | ✅ P0 全绿（team+personal+minimal） |
| 工作区 Go 全量 | `bash scripts/ci-go-packages.sh \| xargs go test -count=1`（≈73 包） | ✅ 0 FAIL（含 scheduler / handler 修复后） |
| proxyctl | `cd apps/proxyctl && GOWORK=off go test ./... -count=1` | ✅ |

### Step 5 顺带修复（原「既有失败」）

| 项 | 处理 |
|----|------|
| `TestModelPriceTable` | 期望价与 `price_table.initDefaultPrices` 对齐；成本用浮点容差 |
| `TestGetSetupStatus_LocalMode` | `t.Setenv` 清空 egress 相关环境变量；增 `TestGetSetupStatus_EgressKeyFromConfig` |
| `scripts/ci-go-packages.sh` | 覆盖 go.work 全模块；跳过 tag-gated / dist / `cmd/centag` |

### Step 4 / Gate 3 复现命令

```bash
cd web && npm run test:ui-caps && npm run lint:ci
make lint harness-check
bash scripts/ci-go-packages.sh | xargs go test -count=1
cd apps/proxyctl && GOWORK=off go test ./... -count=1
```

## 任务验收逐条确认

| 任务 | 验收要点 | 验证方式 | 结果 |
|------|----------|----------|------|
| T1 | `getCapabilities` 四角色关键位 | `test:capabilities` | ✅ |
| T2 | personal/team_user 同源导航；无对话/无配置组 | `test:nav` | ✅ |
| T3 | 全角色 `pipelineTestChat` / admin `liteChatDrawer` | `test:dashboard-sections` + 浏览器 U12 | ✅ |
| T4 | 列表 redirect / 深链放行 /chat·storage | `test:edition-routes` + 浏览器 U14/U15/U16 | ✅ |
| T5 | 无「开始对话→/chat」；首页仍 backends 面板 | `rg` + 浏览器 U10/U11 | ✅ |
| T6 | personal memoryFull；user query；storage 分角色 | selftest + 浏览器 U06–U08；`teamAdminWriteOnly` | ✅ |
| T7 | 本机代理上提；admin 无代理 | `test:nav` + 浏览器 U04/U05 | ✅ |
| T8 | Browser P0；blocked=0 | `/tmp/centag_ui_browser_results.json` | ✅ 44 pass / 0 fail / 3 skip |

## UI 浏览器摘要（T8）

| 部署 | pass / fail / skip / blocked | P0 |
|------|------------------------------|-----|
| team (admin+user) | 26 / 0 / 2 / 0 | ✅ |
| personal | 14 / 0 / 1 / 0 | ✅ |
| minimal | 4 / 0 / 0 / 0 | ✅ |

> U13（测试对话发送）三端均为 skip：抽屉可输入，响应未强断言。可接受残余。

## 风险闭环核对

| 风险 | 状态 | 证据 |
|------|------|------|
| R01–R05 High | 已关闭 | selftest + Playwright JSON |
| R06–R09 | 已关闭 / 设计缓解 | 见 `开发风险评估.md` |
| Critical | **0** | — |

## 人工复核（Gate 4）

- [ ] 抽查 personal / team_user / team_admin 侧栏与流水线「测试」抽屉符合预期  
- [ ] 知悉 U13（发送响应）仍为 skip（抽屉可输入；未强断言远端回复）  
- [ ] 确认可准出 / 合并  

## 备注

- 当前本地服务：`./start.sh debug personal`（`:20060`）可用于抽查。  
- 报告文件：`/tmp/centag_ui_browser_results.json`（会话产物，未入库）。  
