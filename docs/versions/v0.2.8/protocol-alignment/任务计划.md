# 任务计划 — 协议对齐与 Models 接口增强

> 版本：v0.2.8 | 日期：2026-07-22 | 来源方案：`技术方案.md` | 风险评估：`开发风险评估.md`

---

## 执行约束

### 可改文件

- `plugins/protocol/openai/protocol.go` — OpenAI 协议请求/响应/流式结构扩展、ParseRequest 映射
- `plugins/protocol/openai/protocol_test.go` — OpenAI 协议单测
- `plugins/protocol/anthropic/protocol.go` — Anthropic 协议请求/响应/流式结构扩展、ParseRequest 映射、错误格式
- `plugins/protocol/anthropic/protocol_test.go` — Anthropic 协议单测
- `core/pkg/plugin/protocol.go` — ProxyRequest / ErrorResponse 扩展
- `core/pkg/types/types.go` — UnifiedRequest 扩展（与 ProxyRequest 对齐）
- `core/internal/proxy/handler.go` — proxyReq 构造、ListModels 增强
- `core/internal/proxy/list_models_test.go` — ListModels 单测
- `core/internal/cache/proxy_cache.go` — GetRequestKey 签名与 key 生成逻辑
- `core/internal/cache/*_test.go` — 缓存 key 单测
- `core/internal/middleware/proxy.go` — [修订 P1] GetRequestKey 调用点（5 处），T9 签名变更必须同步
- `core/pkg/server/cache_handler.go` — [修订 P1] GetRequestKey 调用点（预测接口 GenerateCacheKey，1 处）

### 不可改文件

- `core/pkg/server/server.go` — 路由已注册 `/v1/models`，不需变更
- `core/pkg/server/backend_handler.go` — 管理端 GetModels 已有 type 过滤，代理端逻辑复用但不改本文件
- `internal/config/*`、`web/*` — 无配置/前端变更
- `plugins/backend/*` — 后端转发以 RawBody + 显式字段覆盖，本轮不改后端插件构造逻辑

### 遵循规范

- `docs/harness/CONVENTIONS.md` — 编码规范
- `docs/harness/ARCHITECTURE.md` — 分层约束（协议插件不依赖 internal/，internal/proxy 不直接引用 plugins/）

---

## 开发风险与任务映射

| 风险 ID | 等级 | 归属任务 | 验收中如何覆盖 |
|---------|------|----------|----------------|
| R01 | High | T2、T5 | 单测：畸形 `response_format` / `tool_choice` 输入 → 断言 RawBody 仍含原始值 |
| R02 | Medium | T3、T6 | 单测：响应序列化 → 断言旧客户端反序列化不报错（简化 struct 模拟） |
| R03 | Medium | T8 | 单测：ListModels 返回数量 → 断言过滤后数量 ≤ 总数；断言 owned_by 非空 |
| R04 | Medium | T7 | 单测：thinking 流式事件 → 断言 SSE 格式含 `thinking_delta` |
| R05 | High | T8 | 单测：Handler 构造 proxyReq → 断言新增字段值传递完整；rg 校验 helper 函数存在 |
| R06 | Low | T4 | 单测：流式最后一块 → 断言 choices:[] + usage 存在；判据为 UsagePromptTokens\|\|UsageCompletionTokens>0 |
| R07 | Medium | T10–T12 | CI：`make test` 全量通过；`make lint` 无新增警告 |
| R08 | Low | T12 | 单测：错误响应序列化 → 断言含 param（有值时）/ 不含 param（空值时） |
| R09 | Low | T8 | 单测：断言 pipeline ID 前缀为 `pipeline.`，后端模型名无前缀 |
| R10 | Low | T8 | 单测：同名模型去重后数量为 1，owned_by 正确 |
| R11 | High | T13 | Review：任务计划中 P2 字段无独立任务项；rg 校验无 P2 字段 ParseRequest 映射 |
| R12 | High | T2 | 单测：vision content 数组 → 断言 String() 不 panic，MarshalJSON 可还原 |
| R13 | High | T9 | 单测：`TestCacheKey_DiffersBy{ResponseFormat,Seed,ToolChoice}` + `TestCacheKey_StableForSameInput` |

> High 风险全部映射；Critical 开放数 = 0。

---

## 任务执行顺序（拓扑排序）

```
T1（核心数据结构）
  ├── T2（OpenAI 请求）
  │     ├── T3（OpenAI 响应）
  │     │     └── T4（OpenAI 流式）
  │     └── T10（OpenAI 测试）
  ├── T5（Anthropic 请求）
  │     ├── T6（Anthropic 响应）
  │     │     └── T7（Anthropic 流式+错误）
  │     └── T11（Anthropic 测试）
  └── T8（代理层 Handler）
        ├── T9（缓存 key）
        └── T12（代理层测试）
              └── T13（全量回归 + Lint）
```

---

## 任务列表

---

### 任务 1：扩展核心数据结构（ProxyRequest / ErrorResponse / ResponseFormatSpec）

| 属性 | 值 |
|------|----|
| 功能点 | 内部统一类型扩展 |
| 前置依赖 | 无（基线任务） |
| 难度 | S |
| 关联风险 | R05（字段遗漏） |
| 验收标准 | `go build ./core/pkg/plugin/ ./core/pkg/types/` 编译通过；新增字段可 JSON 序列化/反序列化 |
| 风险缓解 | 新增字段全部 `omitempty`；不破坏现有结构体内存布局；`ToolChoice` 用 `interface{}` 兼容多种形态 |

**执行步骤**：

1. `core/pkg/plugin/protocol.go`：
   - [修订 P2] **新增 `ToolDefinition` / `FunctionDef` 类型**（plugin 包当前无此类型，`ProxyRequest.Tools` 依赖；与 `core/pkg/types/types.go` 中同名结构对齐）
   - `ProxyRequest` 扩展：`Tools []ToolDefinition`、`ToolChoice interface{}`、`ResponseFormat *ResponseFormatSpec`、`Seed *int`、`N *int`、`User string`、`ParallelToolCalls *bool`、`Modalities []string`（P2 占位）、`TopK int`（P2 占位）
   - 新增 `ResponseFormatSpec` 类型
   - `ErrorResponse` 扩展 `Param string`（`omitempty`）
2. `core/pkg/types/types.go`：
   - `UnifiedRequest` 同步扩展（与 ProxyRequest 对齐，保持两种类型一致）
3. 编译验证：
   ```bash
   go build ./core/pkg/plugin/ ./core/pkg/types/
   ```

---

### 任务 2：OpenAI 协议 — 请求结构扩展与解析（含 vision 修复）

| 属性 | 值 |
|------|----|
| 功能点 | OpenAI ChatCompletions 请求字段补齐 |
| 前置依赖 | T1 |
| 难度 | M |
| 关联风险 | R01（解析错误）、R12（vision 解析不健壮） |
| 验收标准 | `go test ./plugins/protocol/openai/ -run TestParseRequest` 通过；畸形输入单测通过 |
| 风险缓解 | RawBody 始终保留作为 fallback；新增字段解析失败记 Warn 不阻断；vision 数组扩展 `UnmarshalJSON` |

**执行步骤**：

1. `plugins/protocol/openai/protocol.go`：
   - `ChatCompletionRequest` 扩展：[修订 P3] **`Tools []ToolDefinition`**（当前结构体无 Tools 字段，L4 显式映射依赖）、`Seed`、`ResponseFormat`、`ToolChoice`、`N`、`User`、`ParallelToolCalls`、`Modalities`（P2 占位不映射）、`Audio`（P2 占位不映射）、`Metadata`、`ServiceTier`、`Store`、`ReasoningEffort`、`Logprobs`（P2 占位不映射）、`TopLogprobs`（P2 占位不映射）
   - 新增 `ToolDefinition`、`FunctionDef`（OpenAI 形态：`{type:"function",function:{name,description,parameters}}`，映射到 plugin.ToolDefinition）、`ResponseFormat`、`JSONSchemaDef`、`AudioConfig` 类型
   - `Message` 扩展：`Name`、`Refusal`
2. `ParseRequest` 改造：
   - 映射到 `ProxyRequest` **显式字段**（与 T1 结构体对齐）：
     - `Seed` → `ProxyRequest.Seed`
     - `ResponseFormat` → `ProxyRequest.ResponseFormat`
     - `ToolChoice` → `ProxyRequest.ToolChoice`
     - `Tools` → `ProxyRequest.Tools`
     - `N` → `ProxyRequest.N`
     - `User` → `ProxyRequest.User`
     - `ParallelToolCalls` → `ProxyRequest.ParallelToolCalls`
     - `ReasoningEffort` → `ProxyRequest.Reasoning.Effort` + `Specified=true`
     - `ServiceTier`/`Store`/`Metadata` → `ProxyRequest.Metadata[...]`（无显式字段承载）
   - `Modalities`/`Audio`/`Logprobs`/`TopLogprobs` — **本轮不映射**（G7），仅 RawBody 透传
3. `MessageContent` 扩展 `UnmarshalJSON` / `MarshalJSON`：
   - 支持 vision 格式：`type:image_url`、`type:text`、`type:audio` 等数组元素完整保留
   - `String()` 不 panic，返回可读的文本摘要（图片标记为 `[image]`）
4. 单测：
   ```bash
   go test ./plugins/protocol/openai/ -run TestParseRequest -v
   go test ./plugins/protocol/openai/ -run TestMessageContent_Vision -v
   go test ./plugins/protocol/openai/ -run TestParseRequest_MalformedResponseFormat -v
   ```

---

### 任务 3：OpenAI 协议 — 响应结构扩展与补齐（含 usage 修正）

| 属性 | 值 |
|------|----|
| 功能点 | OpenAI ChatCompletions 响应字段补齐 |
| 前置依赖 | T1 |
| 难度 | M |
| 关联风险 | R02（客户端反序列化）、G5（usage 算法） |
| 验收标准 | `go test ./plugins/protocol/openai/ -run TestHandleResponse` 通过；usage 算法单测断言 Prompt+Completion=Total |
| 风险缓解 | 所有新增响应字段 `omitempty`；不伪造数据；usage 从 Metadata 提取 prompt_tokens |

**执行步骤**：

1. `plugins/protocol/openai/protocol.go`：
   - `ChatCompletionResponse` 扩展：`SystemFingerprint`、`ServiceTier`
   - `Choice` 扩展：`Logprobs *ChoiceLogprobs`（P2 占位 `omitempty`）
   - 新增 `ChoiceLogprobs`、`TokenLogprob`、`TopTokenLogprob` 类型（P2 占位）
   - `Usage` 扩展：`PromptTokensDetails *TokenDetails`、`CompletionTokensDetails *CompletionTokenDetails`
   - 新增 `TokenDetails`、`CompletionTokenDetails` 类型
2. `HandleResponse` 改造：
   - `Created` 改为 `time.Now().Unix()`
   - `SystemFingerprint` / `ServiceTier`：从 `ProxyResponse.Metadata` 提取；无则省略
   - **G5 usage 修正**：
     - `PromptTokens` = `resp.Metadata["prompt_tokens"]`（int）；无则 0
     - `CompletionTokens` = `resp.TokensUsed`（或后端路径的 `UsageCompletionTokens`）
     - `TotalTokens = PromptTokens + CompletionTokens`（禁止等于 CompletionTokens 单值）
     - `PromptTokensDetails` / `CompletionTokensDetails`：从 `resp.Metadata` 提取，无则省略
   - `Message.Refusal`：从 `resp.Content` 或 `resp.Metadata["refusal"]` 提取；拒绝时填充
3. 单测：
   ```bash
   go test ./plugins/protocol/openai/ -run TestHandleResponse -v
   go test ./plugins/protocol/openai/ -run TestUsage_TotalEqualsPromptPlusCompletion -v
   go test ./plugins/protocol/openai/ -run TestHandleResponse_SystemFingerprint -v
   ```

---

### 任务 4：OpenAI 协议 — 流式响应扩展（含 usage 结束事件）

| 属性 | 值 |
|------|----|
| 功能点 | OpenAI ChatCompletions 流式 SSE 补齐 |
| 前置依赖 | T3 |
| 难度 | M |
| 关联风险 | R06（usage 事件兼容性）、G6（判据修正） |
| 验收标准 | `go test ./plugins/protocol/openai/ -run TestFormatStreamChunk` 通过；流式最后一块含 `choices:[]` + `usage` |
| 风险缓解 | usage 事件判据用 UsagePromptTokens\|\|UsageCompletionTokens>0（非 TokensUsed>0）；符合 OpenAI 2024-10 官方行为 |

**执行步骤**：

1. `plugins/protocol/openai/protocol.go`：
   - `FormatStreamChunk` 改造：
     - `delta` 扩展：`Refusal`、`ReasoningContent`
     - `ToolCalls` 增量支持（已有基础，确认流式 tool_calls 数组增量格式）
   - **流式 usage 结束事件（G6）**：
     - 判据：`chunk.Done && (chunk.UsagePromptTokens > 0 || chunk.UsageCompletionTokens > 0)`
     - 构造 chunk：`choices: []`、`usage: {prompt_tokens, completion_tokens, total_tokens}`
     - `total_tokens = UsagePromptTokens + UsageCompletionTokens`
     - 在 `[DONE]` 之前发送（作为独立 SSE data 行）
2. `FormatStreamDone` 保持不变（返回 `"[DONE]"`）
3. 单测：
   ```bash
   go test ./plugins/protocol/openai/ -run TestFormatStreamChunk -v
   go test ./plugins/protocol/openai/ -run TestFormatStreamChunk_UsageEvent -v
   go test ./plugins/protocol/openai/ -run TestFormatStreamChunk_ZeroPromptPositiveCompletion -v
   ```

---

### 任务 5：Anthropic 协议 — 请求结构扩展与解析修复（含 RawBody + tool_use_id）

| 属性 | 值 |
|------|----|
| 功能点 | Anthropic Messages 请求字段补齐 + 透传修复 |
| 前置依赖 | T1 |
| 难度 | M |
| 关联风险 | R01（解析错误）、G1（RawBody 破坏透传）、G3（tool_use_id 读取错误）、L4（Tools 显式映射） |
| 验收标准 | `go test ./plugins/protocol/anthropic/ -run TestParseRequest` 通过；tool_use_id 回环单测通过 |
| 风险缓解 | RawBody 存原始 map（非 struct）；ContentBlock 增 ToolUseID；extractToolCallID 读正确字段 |

**执行步骤**：

1. `plugins/protocol/anthropic/protocol.go`：
   - `MessagesRequest` 扩展：`TopK`、`Thinking *ThinkingConfig`、`Metadata *MetadataConfig`、`StreamOptions *StreamOptions`
   - 新增 `ThinkingConfig`、`MetadataConfig`、`StreamOptions` 类型
2. `ParseRequest` 改造（G1）：
   - 先 `json.Unmarshal(body, &rawBody map[string]interface{})`
   - 再 `json.Unmarshal(body, &anthropicReq)`
   - `ProxyRequest.RawBody = rawBody`（map，非 struct）
   - `Thinking` → `ProxyRequest.Reasoning`（`BudgetTokens` + `Specified=true`）
   - `TopK` — **本轮不映射**（G7），仅 RawBody 透传
   - `Metadata.UserID` → `ProxyRequest.Metadata["anthropic_user_id"]`
   - `StreamOptions` → `ProxyRequest.Metadata["stream_options"]`
   - **L4**：`Tools` → `ProxyRequest.Tools`、`ToolChoice` → `ProxyRequest.ToolChoice`
3. **G3 tool_use_id 修复**：
   - `ContentBlock` 增 `ToolUseID string`（`json:"tool_use_id,omitempty"`）
   - `extractToolCallID` 改为读 `block.ToolUseID`（不再读 `block.Input.(string)`）
   - `convertAnthropicMessages` 中 `tool_result` 块的 `ToolCallID` 回填 `ToolUseID`
4. 单测：
   ```bash
   go test ./plugins/protocol/anthropic/ -run TestParseRequest -v
   go test ./plugins/protocol/anthropic/ -run TestToolUseID_Extraction -v
   go test ./plugins/protocol/anthropic/ -run TestParseRequest_RawBodyIsMap -v
   ```

---

### 任务 6：Anthropic 协议 — 响应结构扩展

| 属性 | 值 |
|------|----|
| 功能点 | Anthropic Messages 响应字段补齐 |
| 前置依赖 | T1 |
| 难度 | S |
| 关联风险 | R02（客户端反序列化） |
| 验收标准 | `go test ./plugins/protocol/anthropic/ -run TestHandleResponse` 通过 |
| 风险缓解 | 新增响应字段 `omitempty`；不伪造数据 |

**执行步骤**：

1. `plugins/protocol/anthropic/protocol.go`：
   - `MessagesResponse` 扩展：`StopSequence string`（`omitempty`）
   - `Usage` 扩展：`CacheCreationInputTokens int`、`CacheReadInputTokens int`（均 `omitempty`）
2. `HandleResponse` 改造：
   - `StopSequence`：从 `ProxyResponse.Metadata["stop_sequence"]` 提取；无则省略
   - `CacheCreationInputTokens` / `CacheReadInputTokens`：从 `ProxyResponse.Metadata` 提取（键名 `cache_creation_input_tokens`、`cache_read_input_tokens`）；无则 0/省略
3. 单测：
   ```bash
   go test ./plugins/protocol/anthropic/ -run TestHandleResponse -v
   go test ./plugins/protocol/anthropic/ -run TestHandleResponse_CacheTokens -v
   ```

---

### 任务 7：Anthropic 协议 — 流式 thinking 事件与错误格式修复

| 属性 | 值 |
|------|----|
| 功能点 | Anthropic 流式 thinking 事件 + 错误响应格式重构 |
| 前置依赖 | T6 |
| 难度 | M |
| 关联风险 | R04（thinking 格式变动）、G2（错误结构不正确） |
| 验收标准 | `go test ./plugins/protocol/anthropic/ -run TestFormatStreamChunk` 通过；错误响应单测断言 `error` 为 object |
| 风险缓解 | thinking 事件 index=1（与 text 的 index=0 区分）；错误格式按 Anthropic 官方 `{"type":"error","error":{"type":...,"message":...}}` 重构 |

**执行步骤**：

1. `plugins/protocol/anthropic/protocol.go`：
   - `FormatStreamChunk` 改造：
     - 检测 `chunk.ReasoningContent`，若不为空则输出 thinking 事件流：
       - `content_block_start`（index=1, type=thinking）
       - `content_block_delta`（index=1, delta.type=thinking_delta）
       - `content_block_stop`（index=1）
   - **G2 错误格式重构**：
     ```go
     c.JSON(500, gin.H{
         "type":  "error",
         "error": gin.H{
             "type":    resp.Error.Type,
             "message": resp.Error.Message,
         },
     })
     ```
2. 单测：
   ```bash
   go test ./plugins/protocol/anthropic/ -run TestFormatStreamChunk -v
   go test ./plugins/protocol/anthropic/ -run TestFormatStreamChunk_Thinking -v
   go test ./plugins/protocol/anthropic/ -run TestHandleResponse_ErrorFormat -v
   ```

---

### 任务 8：代理层 Handler 改造（proxyReq 拷贝 + ListModels 增强）

| 属性 | 值 |
|------|----|
| 功能点 | ModeDispatcher 入口字段传递 + 模型发现接口增强 |
| 前置依赖 | T2（OpenAI 请求解析完成）、T5（Anthropic 请求解析完成） |
| 难度 | M |
| 关联风险 | R05（proxyReq 字段遗漏）、R03/R09/R10（Models 接口） |
| 验收标准 | `go test ./core/internal/proxy/ -run TestProxyRequestFieldCopy` 通过；`TestListModels` 通过后缀模型条目 |
| 风险缓解 | 提取 `copyProxyRequestFields` helper 集中管理字段拷贝；ListModels 默认 `type=chat` 过滤防列表过长 |

**执行步骤**：

1. `core/internal/proxy/handler.go`：
   - 新增 `copyProxyRequestFields(req *plugin.ProxyRequest) *plugin.ProxyRequest` helper：
     ```go
     func copyProxyRequestFields(req *plugin.ProxyRequest) *plugin.ProxyRequest {
         return &plugin.ProxyRequest{
             Model:       req.Model,
             Messages:    req.Messages,
             Temperature: req.Temperature,
             MaxTokens:   req.MaxTokens,
             Stream:      req.Stream,
             RawBody:     req.RawBody,
             // P0/P1 显式字段
             Tools:             req.Tools,
             ToolChoice:        req.ToolChoice,
             ResponseFormat:    req.ResponseFormat,
             Seed:              req.Seed,
             N:                 req.N,
             User:              req.User,
             ParallelToolCalls: req.ParallelToolCalls,
             Reasoning:         req.Reasoning,
             // G7: Modalities / TopK 为 P2 占位，本轮不拷贝
         }
     }
     ```
   - 原 `proxyReq := &plugin.ProxyRequest{...}` 处替换为 `proxyReq := copyProxyRequestFields(req)`
2. `ListModels` 增强：
   - 支持 `?type=chat|embedding|all`（默认 `chat`）
   - 在 `pipeline.*` 条目之后，追加所有启用后端的 `SupportedModels`（去重，`owned_by` = 后端名）
   - 复用 `backend_handler.go` 的 `isEmbeddingModel` 逻辑（若该函数不可见则内联等价实现）
3. 单测：
   ```bash
   go test ./core/internal/proxy/ -run TestProxyRequestFieldCopy -v
   go test ./core/internal/proxy/ -run TestListModels -v
   go test ./core/internal/proxy/ -run TestListModels_FilterByType -v
   go test ./core/internal/proxy/ -run TestListModels_Dedup -v
   ```

---

### 任务 9：缓存 key 纳入 P0 字段（不可延后）

| 属性 | 值 |
|------|----|
| 功能点 | 精确/语义缓存 key 生成纳入 response_format / tool_choice / seed |
| 前置依赖 | T1（ProxyRequest 扩展完成）、T8（Handler 字段拷贝完成） |
| 难度 | M |
| 关联风险 | R13（缓存污染）、G4 |
| 验收标准 | `go test ./core/internal/cache/ -run TestCacheKey` 通过；4 条差异/稳定性单测全部通过 |
| 风险缓解 | `tool_choice` 用 `json.Marshal` 统一 hash（nil/字符串/对象）；旧缓存失效属预期，自测记录登记 |

**执行步骤**：

1. `core/internal/cache/proxy_cache.go`：
   - [修订 P4] `GetRequestKey` 采用**方案 B（增参，保持调用方最小改动）**：
     ```go
     func (p *ProxyCache) GetRequestKey(model string, messages []interface{}, temperature float64, maxTokens int, opts ...CacheKeyOption) (string, error)
     ```
     用 functional options（`WithResponseFormat / WithToolChoice / WithSeed`）避免 6 个调用点全部改签名；或定长增参 `responseFormat *plugin.ResponseFormatSpec, toolChoice interface{}, seed *int`（调用方少，可接受全改）。实现时二选一，以编译通过 + 调用点全部同步为准。
   - `keyData` map 追加（nil 时省略，保证无新字段时 key 与旧版一致）：
     - `response_format`：`json.Marshal(responseFormat)`（nil 时省略）
     - `tool_choice`：`json.Marshal(toolChoice)`（nil 时省略）
     - `seed`：`seed` 值（nil 时省略）
2. [修订 P1] 同步全部 6 个调用点（编译验证）：
   - `core/internal/middleware/proxy.go` — 5 处（L291/L659/L726/L773/L1335）：从已解析的 `req map[string]interface{}` 提取 `response_format` / `tool_choice` / `seed` 传入。
   - `core/pkg/server/cache_handler.go` — 1 处（L676 预测接口）：`GenerateCacheKeyRequest` 若无新字段则传 nil（key 与旧版一致，属预期）；如需对齐则在请求结构体补字段（可选，不做不阻塞）。
3. 单测（4 条）：
   ```bash
   go test ./core/internal/cache/ -run TestCacheKey_DiffersByResponseFormat -v
   go test ./core/internal/cache/ -run TestCacheKey_DiffersBySeed -v
   go test ./core/internal/cache/ -run TestCacheKey_DiffersByToolChoice -v
   go test ./core/internal/cache/ -run TestCacheKey_StableForSameInput -v
   ```
4. 调用点一致性检查：
   ```bash
   rg 'GetRequestKey' ./core/internal/cache/ ./core/internal/middleware/ ./core/pkg/server/ ./core/internal/proxy/
   ```

---

### 任务 10：OpenAI 协议插件 — 全量单元测试补全

| 属性 | 值 |
|------|----|
| 功能点 | OpenAI 协议新增字段解析/序列化/流式测试 |
| 前置依赖 | T2、T3、T4 |
| 难度 | M |
| 关联风险 | R07（测试回归） |
| 验收标准 | `go test ./plugins/protocol/openai/` 全部通过 |
| 风险缓解 | 测试用 JSON 字符串比较时忽略未知字段（因响应字段膨胀） |

**执行步骤**：

1. `plugins/protocol/openai/protocol_test.go` 补充：
   - `TestParseRequest_ResponseFormat` — 解析 `{"type":"json_object"}` → 断言 `ProxyRequest.ResponseFormat.Type`
   - `TestParseRequest_ToolChoice_AutoNoneRequired` — 字符串形式
   - `TestParseRequest_ToolChoice_FunctionObject` — 对象形式 `{"type":"function","function":{"name":"..."}}`
   - `TestParseRequest_Seed` — 断言 `ProxyRequest.Seed`
   - `TestParseRequest_N` — 断言 `ProxyRequest.N`
   - `TestParseRequest_User` — 断言 `ProxyRequest.User`
   - `TestParseRequest_ParallelToolCalls` — 断言 `ProxyRequest.ParallelToolCalls`
   - `TestParseRequest_ReasoningEffort` — 断言 `ProxyRequest.Reasoning.Effort` + `Specified`
   - `TestParseRequest_MalformedResponseFormat` — 畸形 JSON → 断言 RawBody 仍含原始值，不返回错误
   - `TestHandleResponse_UsageDetails` — 断言 `PromptTokens + CompletionTokens = TotalTokens`
   - `TestHandleResponse_SystemFingerprint` — 从 Metadata 提取
   - `TestFormatStreamChunk_UsageEvent` — chunk.Done + usage 事件
   - `TestMessageContent_Vision` — vision 数组解析不 panic
2. 运行：
   ```bash
   go test ./plugins/protocol/openai/ -v
   ```

---

### 任务 11：Anthropic 协议插件 — 全量单元测试补全

| 属性 | 值 |
|------|----|
| 功能点 | Anthropic 协议新增字段解析/序列化/流式/错误测试 |
| 前置依赖 | T5、T6、T7 |
| 难度 | M |
| 关联风险 | R07 |
| 验收标准 | `go test ./plugins/protocol/anthropic/` 全部通过 |
| 风险缓解 | 错误响应单测断言 `error` 为 object（G2） |

**执行步骤**：

1. `plugins/protocol/anthropic/protocol_test.go` 补充：
   - `TestParseRequest_Thinking` — 断言 `ProxyRequest.Reasoning.BudgetTokens`
   - `TestParseRequest_RawBodyIsMap` — 断言 `RawBody` 类型为 `map[string]interface{}`
   - `TestParseRequest_ToolsToolChoice` — 断言 `ProxyRequest.Tools` / `ToolChoice`
   - `TestToolUseID_Extraction` — tool_result 块的 `ToolCallID` 正确回填
   - `TestHandleResponse_CacheTokens` — 断言 `CacheCreationInputTokens` / `CacheReadInputTokens`
   - `TestHandleResponse_ErrorFormat` — 断言错误响应 `error` 为 object、含 `type` + `message`
   - `TestFormatStreamChunk_Thinking` — thinking 事件流格式（index=1）
2. 运行：
   ```bash
   go test ./plugins/protocol/anthropic/ -v
   ```

---

### 任务 12：代理层与缓存 — 单元测试补全

| 属性 | 值 |
|------|----|
| 功能点 | Handler 字段拷贝、ListModels、缓存 key 测试 |
| 前置依赖 | T8、T9 |
| 难度 | M |
| 关联风险 | R05、R13、R07、R08 |
| 验收标准 | `go test ./core/internal/proxy/ ./core/internal/cache/` 全部通过 |
| 风险缓解 | proxyReq 字段拷贝完整性断言；缓存 key 差异/稳定性断言 |

**执行步骤**：

1. `core/internal/proxy/` 补充测试：
   - `TestProxyRequestFieldCopy` — 断言 `copyProxyRequestFields` 返回的 proxyReq 含全部 P0/P1 字段
   - `TestListModels_BackendModels` — 断言返回条目含后端模型名（非仅 pipeline）
   - `TestListModels_FilterByType` — `?type=chat` 过滤后无 embedding 模型
   - `TestListModels_Dedup` — 同名模型去重后数量为 1
   - `TestListModels_OwnedBy` — 后端模型 `owned_by` 非空
2. `core/internal/cache/` 补充测试（同 T9 单测清单）
3. 运行：
   ```bash
   go test ./core/internal/proxy/ ./core/internal/cache/ -v
   ```

---

### 任务 13：全量回归测试 + Lint + 文档同步

| 属性 | 值 |
|------|----|
| 功能点 | 质量门禁 + P2 范围控制验证 + API 文档同步 |
| 前置依赖 | T10、T11、T12 |
| 难度 | S |
| 关联风险 | R07（测试回归）、R11（排期膨胀） |
| 验收标准 | `make test` 通过；`make lint` 无新增 warning；rg 校验 P2 字段无 ParseRequest 映射 |
| 风险缓解 | P2 字段范围控制：rg 校验确保无独立映射任务；API 文档同步防止文档与实现脱节 |

**执行步骤**：

1. 全量测试：
   ```bash
   make test
   ```
2. Lint：
   ```bash
   make lint
   ```
3. **R11 P2 范围控制检查**：
   ```bash
   # 以下命令输出应为 0 行（P2 字段不应出现在 ParseRequest 映射或 Handler 拷贝中）
   rg 'ProxyRequest\.(Modalities|TopK|Logprobs)\b' plugins/protocol/openai/protocol.go plugins/protocol/anthropic/protocol.go core/internal/proxy/handler.go
   rg 'req\.Modalities\b|req\.TopK\b|req\.Logprobs\b' plugins/protocol/openai/protocol.go plugins/protocol/anthropic/protocol.go core/internal/proxy/handler.go
   ```
4. 文档同步：
   - 更新 `docs/api/openai-compatible.md`（如有）：新增请求/响应字段说明
   - 更新 `docs/api/models-endpoint.md`（如有）：`/v1/models` 返回结构变更
   - 更新 `docs/guide/proxy-modes.md`（如有）：若 Models 接口行为影响代理模式选择
5. 自测记录：在 `docs/versions/v0.2.8/protocol-alignment/自测记录.md` 登记全量测试结果、旧缓存失效确认

---

## 排期建议

| 任务 | 预估工时 | 优先级批次 |
|------|---------|-----------|
| T1 核心数据结构 | 2h | **Batch 1**（首日） |
| T2 OpenAI 请求 | 4h | **Batch 1**（首日） |
| T3 OpenAI 响应 | 3h | **Batch 1**（首日） |
| T4 OpenAI 流式 | 3h | **Batch 2**（次日） |
| T5 Anthropic 请求 | 4h | **Batch 2**（次日） |
| T6 Anthropic 响应 | 2h | **Batch 2**（次日） |
| T7 Anthropic 流式+错误 | 3h | **Batch 2**（次日） |
| T8 代理层 Handler | 4h | **Batch 3**（第 3 日） |
| T9 缓存 key | 3h | **Batch 3**（第 3 日） |
| T10 OpenAI 测试 | 4h | **Batch 4**（第 3–4 日） |
| T11 Anthropic 测试 | 3h | **Batch 4**（第 3–4 日） |
| T12 代理层/缓存测试 | 3h | **Batch 4**（第 3–4 日） |
| T13 回归 + Lint + 文档 | 3h | **Batch 5**（第 4–5 日） |
| **总计** | **≈ 41h** | **约 5 工作日** |

> 排期假设：1 名熟手 Go 开发者，每日有效工时 8h。若并行多人可压缩 Batch 1/2。
