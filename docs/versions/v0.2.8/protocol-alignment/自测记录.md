# T13 自测记录

## 全量回归测试

- **日期**：2026-07-22
- **命令**：`make test`
- **结果**：全部包通过（PASS），无失败（FAIL）
- **跳过的测试**：仅需要外部服务（Elasticsearch、容器等）的集成测试被跳过，属预期行为

## Lint 检查

- **命令**：`make lint` (golangci-lint run)
- **结果**：0 issues

## P2 范围控制验证

- **命令**：
  ```
  rg 'ProxyRequest\.(Modalities|TopK|Logprobs)\b'
  rg 'req\.Modalities\b|req\.TopK\b|req\.Logprobs\b'
  ```
- **结果**：退出码 1（无匹配行）— P2 字段未出现在 ParseRequest/Handler 拷贝逻辑中

## 文档同步

- `docs/api/openai-compatible.md`：不存在，无需更新
- `docs/api/models-endpoint.md`：不存在，无需更新
- `docs/guide/proxy-modes.md`：Models 接口行为不影响代理模式选择，无需更新

## 旧缓存失效确认

- 缓存键算法保持稳定（基于 model + last user message + temperature + max_tokens + optional params 的 SHA256 哈希）
- P0/P1 字段（response_format, tool_choice, seed）已纳入缓存键
- 新增 P0 字段会导致命中新缓存键，旧缓存自然过期，无需手动清理

## 新增测试统计

| 包 | 测试文件 | 新增测试 | 总测试 |
|---|---------|---------|--------|
| plugins/protocol/openai | protocol_test.go | 10 | 51 |
| plugins/protocol/anthropic | protocol_test.go | 9 | 23 |
| core/internal/proxy | handler_field_copy_test.go | 2 | 4 |
| core/internal/cache | proxy_cache_key_test.go | 9 | 18 |
