# 自测记录 — 本机系统代理出口

> 版本：v0.2.5 | 分支：`feature/v0.2.5` | 日期：2026-07-19

## 自动化已通过（含 Step 4 / Gate 3）

| 项 | 命令 / 说明 | 结果 |
|----|-------------|------|
| T1 `system_proxy.go` | 表驱动 + Normalize/Validate/PAC/LAN；函数覆盖约 **88%–100%** | ✅ |
| T2 setup/status / RefreshPAC | `GetSetupStatus` **100%**；`RefreshPACConfig` **88.9%** | ✅ |
| T2 PAC 组装 | `TestBuildSystemProxyPACConfig_LocalAndLAN` | ✅ |
| T3 engine | mock OS + httptest；覆盖率 **81.1%** | ✅ |
| T3 remote/cli/snapshot | 覆盖率约 **72%–75%**（核心路径已覆盖） | ✅ |
| 前端 lint | `cd web && npm run lint:ci` | ✅ |
| harness-check | `make harness-check`（已允许 `apps/proxyctl`） | ✅ |
| core 全量 | `cd core && go test ./...` | ⚠️ 既有 `TestModelPriceTable` 失败（与本需求无关） |
| golangci-lint | `make lint` | ⚠️ 本机未安装，跳过 |

### Step 4 复现命令

```bash
go test ./core/pkg/config/ ./core/internal/handler/ ./core/pkg/server/ \
  -count=1 -run 'SystemProxy|SetupStatus|RefreshPAC|BuildSystemProxy|ServePAC_|IsLoopback|PACProxy|Normalize|PublicAPI|ValidateSystem'

cd apps/proxyctl && GOWORK=off go test ./internal/engine/ ./internal/remote/ ./internal/cli/ ./internal/snapshot/ -count=1 -cover
```

## 手工验收清单（待勾 / Gate 4）

> 2026-07-19：当前环境不具备完整 Gate 4（跨平台 / 双机），以下保持 ⬜。  
> 联调已确认：OpenCode + 进程级 `HTTPS_PROXY` → MITM → `transparent-proxy` 可正常对话；SSE 透传修复后客户端不再显示裸 `data:`。

### 进程级代理（主路径，多数 Agent）

- [x] 文档写明：首选进程 `HTTPS_PROXY`，勿写全局环境（见 `docs/guide/system-proxy-egress.md`）  
- [x] 代码：非白名单 CONNECT 走 TCP 隧道，不 MITM 解密（`handleCONNECTTunnel`）  
- [ ] 复测：Agent 经 `HTTPS_PROXY` 时 WebFetch/访问非白名单域名仍成功  

### 本机模式（PAC）

- [ ] macOS：`centag-proxyctl enable` → doctor 绿 → 认 PAC 的客户端可用 → `disable` 后原 PAC 恢复  
- [ ] Windows：同上  
- [ ] Linux GNOME：同上；非 GNOME 有明确降级提示  

### 远端 / Team

- [ ] 服务器开启 `allow_lan_clients` + advertise  
- [ ] PAC 正文含 `PROXY <advertise>:8081`，无错误 `PROXY 127.0.0.1`  
- [ ] 员工机 `enable --server http://…:20060` doctor 绿  
- [ ] 员工 `disable` 后服务器 MITM 仍运行  

### 安全

- [ ] 默认 listen loopback；未开 LAN 时无法维持非 loopback  
- [ ] 开 LAN / 全局模式有二次确认  

## 备注

- `apps/proxyctl` 使用独立 `go.mod`，构建：`cd apps/proxyctl && GOWORK=off go build -o centag-proxyctl .`  
- 指南：`docs/guide/system-proxy-egress.md`（**进程代理为主路径**）  

