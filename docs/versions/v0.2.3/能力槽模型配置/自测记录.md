# 自测验证记录 — 能力槽模型配置

> 日期：2026-07-17（Step 4/5 **复跑**） | 分支：`feature/v0.2.3`  
> 审查阶段：Step 5 CR（二次）

## 测试结果

| 测试项 | 命令 | 结果 |
|--------|------|------|
| 前端 Slot 单元断言 | `cd web && npm run test:capability-slots` | ✅（含 cheap/reasoning、关键词冲突、不改源 pipeline） |
| Go 模板契约 | `go test ./pkg/pipeline/ -run TestCapabilitySlotsTemplates_Contract` | ✅ |
| FilePipelineStore 落盘/重启 | `go test ./pkg/pipeline/ -run TestFilePipelineStore_` | ✅（含 ReloadFromDisk） |
| team 写权限中间件 | `go test ./pkg/server/ -run TestTeamAdminWriteOnly` | ✅ |
| pipeline 包回归 | `go test ./pkg/pipeline/` | ✅（~93s） |
| 核心全量测试 | `cd core && go test ./...` | ✅ |
| Go Lint | `make lint` | ✅ 0 issues |
| 前端 Lint | `cd web && npm run lint` | ✅ |
| 卫生检查 | `make harness-check` | ✅ |
| API 验证 | 本需求复用 `PUT /pipelines/:id`；另见同分支 Provider 写权限收紧 | ✅ |

## 验证项逐条确认（对照任务计划）

| # | 验收标准 | 验证方式 | 结果 | 备注 |
|---|---------|---------|------|------|
| T1 | `capabilitySlots.ts` + 发现白名单 / 声明优先 | selftest | ✅ | R01 |
| T1 | 杂散 generator 不被发现 | selftest `Discover_WhitelistExcludesOrphan` | ✅ | |
| T2 | 入口「配置模型」；打开 GET；保存 updatePipeline | 代码审查 HomePipelineCard / PipelineModes / Dialog | ✅ | R06 |
| T2 | 对话框无 routes 编辑 | `rg` CapabilitySlotsDialog 无 routes CRUD | ✅ | R08 |
| T2 | 无双轨业务逻辑 | 旧文件仅 re-export / 薄包装 | ✅ | R03 |
| T3 | 推荐只改草稿、不自动 PUT | Dialog + selftest 不 mutate 输入 | ✅ | R04 |
| T3 | tags code / cheap / reasoning / default | selftest recommend | ✅ | 复跑补齐 |
| T4 | 新增分类写节点+routes+route_config+slots | selftest | ✅ | R11 |
| T4 | 关键词冲突拒绝覆盖 | selftest + `applyAddCategory` 校验 | ✅ | 复跑加固 |
| T4 | 纯函数不改源对象 | selftest | ✅ | |
| T4 | 无 router 时禁用并提示 | PipelineEditorDialog tooltip | ✅ | |
| T5 | router / edu / coding 模板 slots；coding 四阶段 | Go 契约测 | ✅ | R02/R05 |
| T5 | edu 无 bigmodel 硬编码 | Go 契约测 | ✅ | |
| T6 | proxy-modes 职责拆分叙事 | 文档 | ✅ | |
| 回归 | minimal 配置模型落盘 | FilePipelineStore ReloadFromDisk | ✅ | 见 `0b473ab` |
| G5 手工 | 新关键词请求命中节点 | ⬜ 待联调环境 | 人工可选 | 库内旧 coding 需从模板重建 |

## G1–G5 检查表

| 门禁 | 检查项 | 结果 |
|------|--------|:----:|
| G1 | Slot 契约与发现优先级 | ✅ |
| G2 | 统一「配置模型」面板与入口 | ✅ |
| G3 | 按标签智能推荐（草稿） | ✅ |
| G4 | 三样板 YAML 对齐 | ✅ |
| G5 | 画布「新增分类」 | ✅（自动化）；联调请求命中可选 |

## 风险闭环

| 风险 ID | 等级 | 状态 | 验证证据 |
|---------|------|------|----------|
| R01 | High | 已关闭 | selftest 发现白名单 |
| R02 | High | 已关闭 | coding 多阶段契约 + 文档升级说明 |
| R03 | High | 已关闭 | 业务入口仅 capabilitySlots |
| R11 | High | 已关闭 | applyAddCategory 三处一致 + 关键词冲突拒绝 |
| R04–R08/R10 | Med/Low | 已关闭或可接受 | 见开发风险评估.md |

## 问题记录

| 问题 | 状态 | 处理方式 |
|------|------|---------|
| 仓库无 vitest，前端用 tsx selftest | 已接受 | `npm run test:capability-slots` |
| 库内旧单节点 coding 不自动升级 | 已接受（设计） | 文档写明从模板重建 |
| G5 端到端关键词命中 | 待联调 | 需运行中的 personal/gateway 环境 |
| minimal 配置模型不落盘 | 已修复 | `FilePipelineStore` → `data/pipeline-templates/*.yaml` |
| 新增分类关键词静默覆盖旧 route | 已修复（复跑） | 冲突时抛「关键词已存在」 |

## 同分支相关变更（非本需求主范围，已纳入 Gate 3 回归）

| 主题 | Commit / 状态 | 说明 |
|------|---------------|------|
| Provider 统一列表 UI | `e386be9` / `fd1fa3d` | Dashboard/Backends 共用面板；minimal probe-all |
| team 后端写权限仅 admin | 工作区未提交 | `teamAdminWriteOnly` + 面板只读；单测已过 |
