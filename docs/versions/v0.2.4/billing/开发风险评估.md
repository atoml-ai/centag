# 开发风险评估 — 计费功能

> 版本：v0.2.4 | 日期：2026-07-18 | 关联方案：`技术方案.md`  
> 说明：本文件评估**开发实现阶段**风险；High/Critical 必须有应对与任务归属后才能进入编码。  
> 修订：2026-07-18（补齐集成/回归/安全类别；对齐 PricingService 与既有模块边界）  
> Step 3：2026-07-18 实现验证后更新状态

## 1. 评估范围

| 项 | 内容 |
|----|------|
| 需求 | 计费功能（规则引擎、YAML、计量成本归因、管理 API） |
| 分支 | `feature/v0.2.4` |
| 涉及模块 | `core/internal/billing/`（扩展）、`core/internal/tokenusage/`、`core/pkg/server/`、`core/pkg/scheduler/price_table.go`、`core/pkg/database/migrations/`、`config/pricing/` |
| 发行版影响 | minimal / personal / team |
| 是否含数据迁移 / 删旧代码 | 是（`pricing_rules` + `token_usage` 明细列）；不删旧 `billing.Service` / 不 rename `cost_usd` |

## 2. 风险登记表

| ID | 风险描述 | 类别 | 等级 | 影响面 | 概率 | 应对策略 | 归属任务 | 状态 |
|----|----------|------|------|--------|------|----------|----------|------|
| R01 | 价格缓存与规则变更不一致 | 实现 | High | 全模式 | 中 | TTL + `InvalidateCache`；规则写路径强制失效 | T3, T5 | 已关闭（实现验证） |
| R02 | MemoryRuleStore 并发读写竞争 | 实现 | Medium | minimal | 中 | `sync.RWMutex`；单测并发读写 | T2 | 已关闭（实现验证） |
| R03 | SQLite 规则表写入锁导致管理 API 抖动 | 性能 | Medium | personal | 低 | WAL；规则写低频可接受 | T2 | 已缓解（设计） |
| R04 | 迁移 / ephemeral schema 漏列导致写入失败 | 迁移 | High | 全模式 | 中 | 031/032 + ephemeral 同步改；幂等迁移单测 | T1, T4 | 已关闭（实现验证） |
| R05 | 规则 vs 硬编码 fallback 优先级混乱 | 实现 | High | 全模式 | 中 | 明确 rule > legacy_table > default；日志记 Source | T3, T4 | 已关闭（实现验证） |
| R06 | 通配符/优先级边界 case 多 | 实现 | Medium | 全模式 | 中 | 单测矩阵覆盖精确/通配/同分 | T3 | 已关闭（实现验证） |
| R07 | 与现有 `billing.Service`（Event）类型/职责混淆 | 集成 | High | team | 中 | 新类型统一 `PricingService`/`RuleStore`；禁止复用 Event Service 做定价 | T2, T3 | 已关闭（实现验证） |
| R08 | 范围膨胀（钱包/auth 预算/平行 costs API/Web） | 排期 | Medium | 整体 | 中 | 对照非目标清单做 T6 验收 | T6 | 已关闭（实现验证） |
| R09 | 新建 `/admin/billing/costs*` 与既有 `/admin/cost/summary` 双入口 | 集成 | Medium | 运维/文档 | 中 | 只增强既有 cost API；规则走 `/billing/rules` | T5, T6 | 已关闭（实现验证） |
| R10 | 计量主路径回归（多记/阻断/成本翻倍） | 回归 | High | 代理主路径 | 中 | Pricing 失败时 fallback 旧表；不阻断请求；集成测断言单次写入 | T4 | 已关闭（实现验证） |
| R11 | 规则管理 API 鉴权缺失导致越权改价 | 安全 | High | admin API | 中 | 挂既有 admin 路由组；单测未授权 401/403 | T5 | 已缓解（设计） |
| R12 | 显示汇率被误写入落库金额 | 实现 | Medium | 全模式 | 中 | 正本始终 USD；仅 UI 用 `usd_to_cny`；导入 CNY YAML 才换算入库 | T4, T5 | 已关闭（USD 本位 + 显示换算） |

> R11：路由已挂 `JWT + AdminOnly`（team/personal admin）/ minimal 受保护组；handler 层不单独鉴权，依赖中间件（与既有 CostHandler 一致）。残余可接受。

## 3. Critical / High 专项

### R01 — 价格缓存与规则变更不一致

- **验证方式**：`go test ./core/internal/billing/... -run TestPricingCacheInvalidation` ✅
- **残余风险**：可接受（TTL 窗口偏差）

### R04 — 迁移 / ephemeral 漏列

- **验证方式**：`go test ./core/pkg/database/ -run Migration031`；`go test ./core/internal/tokenusage/... -run Ephemeral` ✅
- **残余风险**：可接受

### R05 — fallback 优先级混乱

- **验证方式**：`go test ./core/internal/billing/... -run TestPriceResolution` ✅
- **残余风险**：可接受

### R07 — 与 Event Service 混淆

- **验证方式**：`go test ./core/pkg/server/... -run BillingHook`；定价类型为 `PricingService` ✅
- **残余风险**：可接受

### R10 — 计量主路径回归

- **验证方式**：`go test ./core/internal/tokenusage/...` ✅
- **残余风险**：可接受

### R11 — 规则 API 越权

- **缓解措施**：注册于 `adminAPI`（JWT + AdminOnly）/ minimal JWT 保护组
- **残余风险**：可接受（依赖既有 admin 中间件）

## 4. 与任务计划的映射

| 风险 ID | 任务 ID | 验收中如何覆盖 |
|---------|---------|----------------|
| R01 | T3, T5 | 缓存失效单测 + 规则写后立即查询 |
| R02 | T2 | MemoryRuleStore 并发单测 |
| R03 | T2 | 文档约束 + 可选 SQLite 并发测 |
| R04 | T1, T4 | 迁移幂等 + ephemeral 字段写入 |
| R05 | T3, T4 | Source/优先级单测 + 集成测 |
| R06 | T3 | 通配符矩阵单测 |
| R07 | T2, T3 | 类型命名审查 + Event hook 回归测 |
| R08 | T6 | 对照技术方案 §1.3 非目标清单 |
| R09 | T5, T6 | 路由清单无平行 costs；文档只描述既有 cost API |
| R10 | T4 | 失败 fallback + 单次写入断言 |
| R11 | T5 | 路由挂 admin 中间件 |
| R12 | T4, T5 | 无汇率代码；响应含 currency=CNY |

## 5. 结论

| 项 | 结论 |
|----|------|
| Critical 开放数 | **0** |
| High 开放数 | **0**（实现验证关闭；R11 设计缓解可接受） |
| 是否建议进入任务规划 / 编码 | Step 3 已完成，可 `step4-test` / `step5-review` |

## 6. 变更记录

| 日期 | 变更 | 作者 |
|------|------|------|
| 2026-07-17 | 初版 | AI |
| 2026-07-18 | 复审优化：分支名、路径、补 R07–R12、High 专项与映射 | AI |
| 2026-07-18 | Step 3 实现验证后关闭 R01–R10/R12 | AI |
