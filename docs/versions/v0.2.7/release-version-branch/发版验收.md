# 发版验收 — v0.2.7（Step 6，Path B 重发）

> 日期：2026-07-21 | 需求：release-version-branch | 路径：B（重建并上传）

## 参数

| 项 | 值 |
|----|-----|
| 版本 | `0.2.7`（tag `v0.2.7` → `1771b1b`） |
| 仓库 | `atoml-ai/centag`（**public**） |
| 分支门禁 | ✅ `feature/v0.2.7` |
| Gate 4 | ✅ 人工批准可发版 |
| Release | https://github.com/atoml-ai/centag/releases/tag/v0.2.7 |
| isDraft | `false`（公开） |
| 验收 | 仅资产（安装冒烟跳过） |

## 执行摘要

1. Path B：`publish-binaries.sh --version 0.2.7 --release` 重建并上传 13 资产。
2. tag 强制更新至含默认密码 `centag123` 的 commit。
3. Release notes：Install（钉版本）+ Default login + Uninstall。
4. **安装冒烟：跳过**（用户手动）。

## G5 检查

| # | 检查 | 结果 |
|---|------|------|
| G5.1 | Release 公开 | ✅ |
| G5.2 | 资产齐全（13） | ✅ |
| G5.3 | 版本分支门禁 | ✅ |
| G5.4 | 安装冒烟 | ⏭ 用户手动 |

## 默认登录（首轮）

- 用户名：`admin`
- 密码：`centag123`

## 用户安装命令

```bash
curl -fsSL https://raw.githubusercontent.com/atoml-ai/centag/v0.2.7/scripts/install.sh \
  | bash -s 0.2.7 && . "$HOME/.centag/env"
```

## 结论

- Step 6 / Gate 5：**通过**
