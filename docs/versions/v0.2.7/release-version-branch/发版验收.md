# 发版验收 — v0.2.7（Step 6，wrap 重命名重发）

> 日期：2026-07-21 | 需求：release-version-branch | 路径：B→A（重建产物后上传）

## 参数

| 项 | 值 |
|----|-----|
| 版本 | `0.2.7`（tag `v0.2.7` → `5d7000c`） |
| 仓库 | `atoml-ai/centag`（**public**） |
| 分支门禁 | ✅ `feature/v0.2.7` |
| Gate 4 | ✅ 人工批准可发版 |
| Release | https://github.com/atoml-ai/centag/releases/tag/v0.2.7 |
| isDraft | `false`（已 Publish） |

## 执行摘要

1. Path B：`build-artifacts.sh --version 0.2.7 --components personal,wrap` → `bin/release/0.2.7/`。
2. Path A：`gh release upload --clobber` 上传 personal + wrap + checksums。
3. 删除旧资产 `centag-proxyctl-*`（6 个）。
4. 强制移动 tag `v0.2.7`：`1729550` → `5d7000c`（含 install.sh / wrap 重命名）。
5. **安装冒烟：跳过**（用户手动部署验收）。

## G5 检查

| # | 检查 | 结果 | 证据 |
|---|------|------|------|
| G5.1 | Release 已创建且公开 | ✅ | isDraft=false，URL 如上 |
| G5.2 | 资产齐全 | ✅ | 13 项：personal×6 + wrap×6 + checksums |
| G5.3 | 版本分支门禁 | ✅ | require-release-branch OK |
| G5.4 | 安装冒烟 | ⏭ | 默认跳过；用户手动 |

## 资产清单

- `centag-personal-{darwin,linux,windows}-{amd64,arm64}.tar.gz`
- `centag-wrap-{darwin,linux,windows}-{amd64,arm64}.tar.gz`
- `checksums.txt`

## 用户安装命令

```bash
unset CENTAG_RELEASE_BASE
curl -fsSL https://raw.githubusercontent.com/atoml-ai/centag/v0.2.7/scripts/install.sh \
  | bash -s -- --version 0.2.7

# 仅 wrap：
# | bash -s -- --only wrap --version 0.2.7
```

## 结论

- Step 6 / Gate 5：**通过**（Release 公开、资产齐全、tag 已对齐；安装由用户手动）
