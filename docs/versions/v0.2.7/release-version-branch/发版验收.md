# 发版验收 — v0.2.7（Step 6，完整重发）

> 日期：2026-07-21 | 需求：release-version-branch | 路径：B（重建并上传）

## 参数

| 项 | 值 |
|----|-----|
| 版本 | `0.2.7`（tag `v0.2.7` → `53bdf0c`） |
| 仓库 | `atoml-ai/centag`（**public**） |
| 分支门禁 | ✅ `feature/v0.2.7` |
| Gate 4 | ✅ 人工批准可发版 |
| Release | https://github.com/atoml-ai/centag/releases/tag/v0.2.7 |
| isDraft | `false`（公开） |
| 验收 | 仅资产（安装冒烟跳过） |

## 执行摘要

1. **删旧**：`gh release delete v0.2.7`（不清理 tag）；删本地+远端 tag `v0.2.7`（原指向 `71f4800`）。
2. **重打 tag**：在 HEAD `53bdf0c` 上 `git tag v0.2.7 && git push origin v0.2.7`（首次推送遇 GitHub 500 瞬时错误，重试成功）。
3. **CI 触发**：tag 推送触发 `.github/workflows/release.yml`，由 `github-actions[bot]` 创建草稿 Release（含英文标准 notes 模板）。
4. **Path B 构建**：`build-artifacts.sh --version 0.2.7 --components personal` 交叉编译 6 平台 personal CLI（含 `centag wrap` 子命令 + WebUI 静态资源） + `checksums.txt`，输出 `~/.centag/var/release/0.2.7/`。
5. **上传覆盖**：`publish-binaries.sh --version 0.2.7 --release` 检测到 CI 已建草稿 Release，用 `gh release upload --clobber` 以本地产物覆盖 CI 资产；远端 `checksums.txt` 与本地一致（已 diff 校验）。
6. **公开**：`gh release edit v0.2.7 --draft=false` → `published: 2026-07-21T14:47:13Z`。
7. **资产范围**：personal only（7 项 = 6 平台 tar.gz + checksums）。`centag wrap` 已嵌入 `centag` 主二进制为子命令，**不再**单独发布 `centag-wrap-*.tar.gz`（与现行 procedure「范围」一致；上轮 13 资产布局已废止）。
8. **安装冒烟：跳过**（用户手动）。

## G5 检查

| # | 检查 | 结果 |
|---|------|------|
| G5.1 | Release 公开 | ✅ `draft: false` |
| G5.2 | 资产齐全（7=6 平台 personal + checksums） | ✅ |
| G5.3 | 版本分支门禁（`require-release-branch.sh`） | ✅ `feature/v0.2.7` |
| G5.4 | 远端 `checksums.txt` 与本地一致 | ✅ diff MATCH |
| G5.5 | 安装冒烟 | ⏭ 用户手动 |

## 默认登录（首轮）

- 用户名：`admin`
- 密码：`centag123`

## 用户安装命令

```bash
curl -fsSL https://raw.githubusercontent.com/atoml-ai/centag/v0.2.7/scripts/install.sh \
  | bash -s 0.2.7 && . "$HOME/.centag/env"
```

## 备注

- tag `v0.2.7` 现指向 `53bdf0c`（与原 `71f4800` / 更早 `1771b1b` 不同）；包含 transparent-responses-wrap 之后的所有变更。
- CI 的 `release.yml` 会因 tag 推送自动跑（5m15s，已 success）；本地 Path B 上传覆盖 CI 资产以 ldflags/构建参数与本仓库脚本对齐。
- 若后续需保留 CI-only 路径，可不再本地 `--release`，仅推 tag 由 CI 完成（Path C）。

## 结论

- Step 6 / Gate 5：**通过**