# 发版验收 — v0.2.7（Step 6，Path B 重发）

> 日期：2026-07-21 | 需求：release-version-branch | 路径：B（重建并上传）

## 参数

| 项 | 值 |
|----|-----|
| 版本 | `0.2.7`（tag `v0.2.7` → `e273272`） |
| 仓库 | `atoml-ai/centag`（**public**） |
| 分支门禁 | ✅ `feature/v0.2.7` |
| Gate 4 | ✅ 人工批准可发版 |
| Release | https://github.com/atoml-ai/centag/releases/tag/v0.2.7 |
| isDraft | `false`（公开） |
| 验收 | 仅资产（安装冒烟跳过） |

## 执行摘要

1. Path B：`publish-binaries.sh --version 0.2.7 --release` 重建 personal+wrap × 6 平台 + checksums 并 `--clobber` 上传。
2. `gh release edit --draft=false` + 同步英文 Install/Uninstall notes（含 `&& source ~/.centag/env`）。
3. **安装冒烟：跳过**（用户手动）。

## G5 检查

| # | 检查 | 结果 | 证据 |
|---|------|------|------|
| G5.1 | Release 已创建且公开 | ✅ | isDraft=false |
| G5.2 | 资产齐全 | ✅ | 13 项：personal×6 + wrap×6 + checksums |
| G5.3 | 版本分支门禁 | ✅ | require-release-branch OK |
| G5.4 | 安装冒烟 | ⏭ | 默认跳过；用户手动 |

## 用户安装命令

```bash
curl -fsSL https://raw.githubusercontent.com/atoml-ai/centag/v0.2.7/scripts/install.sh \
  | bash -s -- --version 0.2.7 && source "$HOME/.centag/env"
```

## 结论

- Step 6 / Gate 5：**通过**（公开、资产齐全；安装由用户手动）
