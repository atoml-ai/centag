# 发版验收 — v0.2.7（Step 6，公开仓重发）

> 日期：2026-07-21 | 需求：release-version-branch | 路径：B→A（重建产物后上传）

## 参数

| 项 | 值 |
|----|-----|
| 版本 | `0.2.7`（tag `v0.2.7`） |
| 仓库 | `atoml-ai/centag`（**public**） |
| 分支门禁 | ✅ `feature/v0.2.7` |
| Gate 4 | ✅ 人工批准可发版 |
| Release | https://github.com/atoml-ai/centag/releases/tag/v0.2.7 |
| isDraft | `false`（已 Publish） |

## 执行摘要

1. 旧 Release / 本地 `bin/release/0.2.7` 已不存在 → Path B 重建。
2. `publish-binaries.sh --release` 因 `build-artifacts` stdout 混有 npm 日志导致 OUT_DIR 解析失败（已修：`tail -n1`）。
3. Path A：`gh release create --draft` 上传 13 资产 → `gh release edit --draft=false`。

## G5 检查

| # | 检查 | 结果 | 证据 |
|---|------|------|------|
| G5.1 | Release 已创建且公开 | ✅ | isDraft=false，URL 如上 |
| G5.2 | 资产齐全 | ✅ | 13 项 |
| G5.3 | 版本分支门禁 | ✅ | require-release-branch OK |
| G5.4 | 安装冒烟 | ⚠️ | 见下 |

### URL 探测（公开）

- `raw…/v0.2.7/scripts/install.sh` → **HTTP 200**
- `releases/download/v0.2.7/checksums.txt` → **HTTP 302**（可匿名下载）

### `install.sh` 冒烟（本机）

- `unset CENTAG_RELEASE_BASE` 后执行。
- personal 下载时 `github.com:443` **超时** → 回退 from-source；二进制最终安装成功。
- 属本机出口网络波动，非资产缺失；公开 URL 探测已通过。

## 用户安装命令

```bash
unset CENTAG_RELEASE_BASE
curl -fsSL https://raw.githubusercontent.com/atoml-ai/centag/v0.2.7/scripts/install.sh \
  | bash -s -- --version 0.2.7
```

## 结论

- Step 6 / Gate 5：**通过**（Release 已公开、资产齐全；本机 curl 偶发超时记残余）
