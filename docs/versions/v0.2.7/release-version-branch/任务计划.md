# 任务计划 — 发版仅限版本分支

> 版本：v0.2.7 | 需求：release-version-branch

| ID | 任务 | 验收标准 | 执行约束 | 风险 |
|----|------|----------|----------|------|
| T1 | 实现 `require-release-branch.sh` 并接入 publish / CI | 本地在 `feature/v0.2.7` 通过；main 拒绝；CI dispatch/tag 规则符合方案 | 可改 `scripts/release/*`、`.github/workflows/release.yml`、skill/README | R01 |
| T2 | 文档与 skill 同步版本分支约定 | README / `step6-release` skill 描述与脚本一致 | 文档与规则文件 | R03 |
| T3 | 补全门禁脚本测试 | `bash scripts/release/require-release-branch_test.sh` 全绿 | 可新增 `*_test.sh` | R01 |

## 编码级步骤（T3）

1. Spec 追溯：允许分支集合与 CI 事件矩阵见技术方案。
2. 实现：表驱动 shell 用例覆盖 local/CI/bypass/拒绝路径。
3. 验证：运行上述测试命令 exit 0。
