# 开发风险评估 — 透明代理 Responses 桥接与 wrap 内嵌

> 版本：v0.2.7 | 需求：transparent-responses-wrap（合入夹带改动补登记） | 日期：2026-07-21

## 登记表

| ID | 级别 | 描述 | 应对 | 验证方式 | 状态 |
|----|------|------|------|----------|------|
| R01 | High | OpenCode `/v1/responses` 透明转发把 `input` 原样打到上游 `/chat/completions`，导致「输入不能为空」 | `responses_chat_bridge`：Responses→Chat 请求改写；成功响应抽文本/tool_calls 后走 Responses SSE/非流式信封 | `go test ./core/pkg/pipeline/ ./core/internal/proxy/ -run 'Responses\|Transparent\|ToolCalls'`；OpenCode 实网一轮对话+工具 | 已缓解（实现） |
| R02 | High | Agent 工具轮次：flat tools / `function_call` / `function_call_output` 丢失，或 chat `tool_calls` 未映射为 Responses 事件 | 请求侧 tools/tool_choice/input 映射；`responsesStreamFormatter` + 非流式 `writeResponsesAPIJSON` 发 function_call | 同上单测 + OpenCode 工具调用 | 已缓解（实现） |
| R03 | Medium | 非流式 `/v1/responses` 静默返回 `chat.completion` JSON，协议错误 | `writeResponse` 按 `protocol_plugin==responses-protocol` 输出 Responses envelope | `TestDispatch_NonStream_ResponsesProtocolEnvelope` | 已缓解（实现） |
| R04 | Medium | `centag wrap` 文档仍写「不入 go.work」，与嵌入子命令事实冲突，后续删除 wrap 模块会破坏发行版 | 同步 `ARCHITECTURE.md` / `AGENTS.md` §7；`apps/wrap` 保留在 `go.work` | 文档审查；`go list` / `centag wrap -h` | 已缓解（文档） |
| R05 | Low | `_centag_meta` SSE 因 UA 含 `llm-proxy` 子串误注入第三方严格客户端 | `shouldInjectCentagMetaSSE` 仅 `X-Centag-Include-Meta` 或 UA 前缀 `centag/` / `opencode-centag/` | `TestShouldInjectCentagMetaSSE` | 已缓解（实现） |
| R06 | Low | MITM 默认域名 / 透明路由误打回原厂 Host 导致 AuthError（历史相关） | `ResolveTransparentTargetURL` 优先配置后端；域名白名单按 API host | 既有 mitm / transparent 测试 | 已关闭（既有） |
| R07 | Low | 默认/种子密码与本地 secrets 混用风险（发版/演示） | secrets 示例占位；安装文档提示改密 | 发版 checklist / profile 文档 | 开放（运维约定） |

## Critical / High 专项

- Critical 开放数：0
- High：R01/R02 由桥接与单测覆盖；实网 OpenCode 建议合入前再手测一轮。

## 结论

可与 v0.2.7 其它改动一并合入；无开放 Critical。
