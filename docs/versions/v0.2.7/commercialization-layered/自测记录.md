# 自测验证记录 — 商业化分层（插件式 Open Core）

> 日期：2026-07-21 | 分支：feature/v0.2.7（centag + centag-pro 同名）

## 测试结果

| 测试项 | 命令 | 结果 |
|--------|------|------|
| 原语 / Host / server | `go test ./core/pkg/{extension,editionmodule,server,authapi,quotaapi,billingasync,abevalapi,systemupdateapi,tokenusageapi,agentapi}/ -count=1` | ✅ 全部通过 |
| 私有边禁入 | `bash scripts/ci-check-no-private-deps.sh` | ✅ |
| pro 冒烟 | `ASSEMBLE_OUT=/tmp/centag-team-gate ./verify.sh`（pro） | ✅ |
| Team 前端 | `./scripts/build-web-team.sh`（pro） | ✅（chunk 含 Users/Tenant/AB/SystemUpdate） |
| personal 构建 | `./start.sh build personal` | ✅ |
| OSS 拒 team | `./start.sh build team` | ✅ exit 1 |
| Web lint | `cd web && npm run lint:ci` | ✅ |
| harness-check | `make harness-check` | ⚠️ 既有路径告警（`var/`、`apps/proxyctl-npm`），非本需求引入 |
| pro Docker | `bash scripts/docker-build-team.sh` → `centag-team:dev` | ✅ 镜像构建成功 |

## 验证项逐条确认

| # | 验收标准 | 验证方式 | 结果 | 备注 |
|---|---------|---------|------|------|
| 1 | 开源无 `pkg/teamadmin`、无私有模块 import | ci-check + rg | ✅ | D6 / R01 / R14 |
| 2 | Host 可挂载；空 Host 零路由 | R09 单测 | ✅ | E2.7 |
| 3 | Team 实现在 pro `internal/teamadmin` | pro 包测试 + 目录 | ✅ | E2R |
| 4 | pro 无 `centag/core/internal` import | verify.sh | ✅ | |
| 5 | E3 pack + D1 `/cost` | edition.ts + build-web-team | ✅ | |
| 6 | E4 CI / example 插件 | workflow + `plugins/example` 测试 | ✅ | |
| 7 | server 单测不再因 nil logger panic | TestMain + `go test ./core/pkg/server` | ✅ | Step4/3 |
| 8 | debug_pid 退出 unbound | start.sh cleanup 幂等 + 非 local PID | ✅ | pro/OSS |
| 9 | pro Docker 发行路径 | Dockerfile.team + start.sh docker build | ✅ | R12 |

## 问题记录

| 问题 | 状态 | 处理方式 |
|------|------|---------|
| `TestWireCircuitBreaker` / MCP 测试 nil logger panic | 已修复 | `core/pkg/server/main_test.go` TestMain 初始化 logger |
| pro `debug team` 退出 `vite_pid: unbound variable` | 已修复 | cleanup 清 trap；PID 用全局 + `${var:-}` |
| `tokenusageapi.Wrap(nil)` 可能落到 nil DefaultService | 已加固 | adapter 方法 nil-safe |
| harness-check 既有 UNEXPECTED PATH | 待处理（非本需求） | 记录残余；不阻塞本需求 Gate |
