name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version without v prefix (e.g. 0.2.7). Empty = tag or package.json'
        required: false
        default: ''
      draft:
        description: 'Create draft release'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  # Version-branch gate: workflow_dispatch / tags must match vX | feature/vX | release/vX.
  guard-branch:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Resolve version
        id: ver
        run: |
          if [[ -n "${{ inputs.version }}" ]]; then
            V="${{ inputs.version }}"
          elif [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            V="${GITHUB_REF_NAME#v}"
          else
            V="$(sed -n 's/.*"version"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' apps/wrap-npm/package.json | head -1)"
          fi
          V="${V#v}"
          echo "version=$V" >> "$GITHUB_OUTPUT"
          echo "Release version v$V"

      - name: Require version branch
        run: bash scripts/release/require-release-branch.sh --ci --version "${{ steps.ver.outputs.version }}"

  # Default release set: personal CLI only (includes `centag wrap` subcommand).
  # build-artifacts writes under $CENTAG_INSTALL_ROOT/var/release/<ver>/
  # (same layout as local ~/.centag; CI pins INSTALL_ROOT into the workspace).
  binaries:
    needs: [guard-branch]
    runs-on: ubuntu-latest
    env:
      CENTAG_INSTALL_ROOT: ${{ github.workspace }}/.centag
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Build personal
        run: |
          bash scripts/release/build-artifacts.sh \
            --version "${{ needs.guard-branch.outputs.version }}" \
            --components personal

      - name: Upload artifact bundle
        uses: actions/upload-artifact@v5
        with:
          name: centag-binaries-${{ needs.guard-branch.outputs.version }}
          path: .centag/var/release/${{ needs.guard-branch.outputs.version }}/
          if-no-files-found: error

  publish:
    needs: [guard-branch, binaries]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Resolve version
        id: ver
        run: |
          echo "version=${{ needs.guard-branch.outputs.version }}" >> "$GITHUB_OUTPUT"

      - name: Download artifacts
        uses: actions/download-artifact@v5
        with:
          path: release-staging

      - name: Flatten assets
        id: assets
        run: |
          V="${{ steps.ver.outputs.version }}"
          # Staging dir for gh release upload (not the install-root layout).
          OUT="release-assets/${V}"
          mkdir -p "$OUT"
          find release-staging -type f \( -name 'centag-*.tar.gz' -o -name 'checksums.txt' \) -exec cp -f {} "$OUT/" \;
          : > "$OUT/checksums.txt"
          (
            cd "$OUT"
            for f in centag-*.tar.gz; do
              [[ -f "$f" ]] || continue
              printf '%s  %s\n' "$(sha256sum "$f" | awk '{print $1}')" "$f" >> checksums.txt
            done
          )
          ls -lh "$OUT"
          echo "dir=$OUT" >> "$GITHUB_OUTPUT"

      - name: Create / update GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          V="${{ steps.ver.outputs.version }}"
          TAG="v${V}"
          OUT="${{ steps.assets.outputs.dir }}"
          DRAFT_FLAG="--draft"
          if [[ "${{ github.event_name }}" == "push" ]]; then
            DRAFT_FLAG="--draft"
          elif [[ "${{ inputs.draft }}" == "false" ]]; then
            DRAFT_FLAG=""
          fi

          # Keep all lines indented under `run: |` — unindented heredoc breaks YAML parsing.
          NOTES_FILE="$(mktemp)"
          {
            echo "## Centag ${TAG}"
            echo ""
            echo "### Install"
            echo ""
            echo '```bash'
            echo "# npm (recommended for Node.js developers)"
            echo "npm install -g centag"
            echo ""
            echo "# curl (one-line installer)"
            echo "curl -fsSL https://raw.githubusercontent.com/${REPO}/${TAG}/scripts/install.sh | bash -s ${V} && . \"\$HOME/.centag/env\""
            echo '```'
            echo ""
            echo "Default install root: \`~/.centag\`. Chain \`. ~/.centag/env\` so PATH applies in this shell."
            echo "Release ships **personal** only — process proxy is \`centag wrap …\` (no separate wrap tarball)."
            echo ""
            echo "### Default login (first run)"
            echo ""
            echo "- Username: \`admin\`"
            echo "- Password: \`centag123\`"
            echo "  (override with \`LLM_PROXY_ADMIN_PASSWORD\` before first start)"
            echo ""
            echo "### Wrap / process proxy (API key)"
            echo ""
            echo "\`centag wrap\` does not start the gateway. When setup/status returns 401:"
            echo ""
            echo '```bash'
            echo "export CENTAG_WRAP_TOKEN='your_api_key'"
            echo "centag wrap doctor"
            echo "centag wrap run -- opencode"
            echo '```'
            echo ""
            echo "### Uninstall"
            echo ""
            echo "No dedicated uninstall command yet — remove manually:"
            echo ""
            echo '```bash'
            echo 'centag wrap disable 2>/dev/null || true'
            echo "pkill -f 'centag-personal|/\\.centag/bin/centag' 2>/dev/null || true"
            echo 'rm -rf "${HOME}/.centag"'
            echo '# Also remove PATH line from ~/.zshrc / ~/.bashrc if present:'
            echo '#   export PATH="$HOME/.centag/bin:$PATH"'
            echo '```'
            echo ""
            echo "### Artifacts"
            echo ""
            echo "- \`centag-personal-<goos>-<goarch>.tar.gz\` — personal CLI + WebUI static (\`centag\` / \`centag wrap\`)"
            echo "- \`checksums.txt\` — SHA-256 sums"
          } > "$NOTES_FILE"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$OUT"/centag-*.tar.gz "$OUT"/checksums.txt --clobber
          else
            # shellcheck disable=SC2086
            gh release create "$TAG" $DRAFT_FLAG \
              --title "Centag ${V}" \
              --notes-file "$NOTES_FILE" \
              "$OUT"/centag-*.tar.gz "$OUT"/checksums.txt
          fi

  npm-publish:
    needs: [guard-branch, publish]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: web/package-lock.json
          registry-url: 'https://registry.npmjs.org'

      - name: Build & pack npm packages
        run: |
          DRY_RUN=1 bash scripts/publish-centag-npm.sh

      - name: Publish to npm
        if: "!github.event.inputs.draft"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.CENTAG_NPM_TOKEN }}
        run: |
          NPM_DIR="apps/centag-npm"
          VENDOR_DIR="${NPM_DIR}/bin/vendor"
          mkdir -p "${VENDOR_DIR}"

          # Cross-compile binaries for all platforms
          PLATFORMS="darwin-amd64 darwin-arm64 linux-amd64 linux-arm64 windows-amd64 windows-arm64"
          TAGS="protocol_openai,protocol_anthropic,protocol_gemini,protocol_openairesponses,backend_openai,backend_ollama,backend_anthropic,backend_gemini,backend_azure"
          VERSION="${{ needs.guard-branch.outputs.version }}"

          for p in $PLATFORMS; do
            goos="${p%-*}"; goarch="${p##*-}"
            ext=""; [[ "$goos" == "windows" ]] && ext=".exe"
            out="${VENDOR_DIR}/${p}/centag-personal${ext}"
            mkdir -p "$(dirname "$out")"
            echo "==> build centag-personal ${p}"
            (
              cd dist/personal
              GOWORK=off CGO_ENABLED=0 GOOS="${goos}" GOARCH="${goarch}" \
                go build -trimpath -tags "$TAGS" -ldflags="-s -w" -o "$(realpath "$out")" .
            )
          done

          # Copy config seed data
          for p in $PLATFORMS; do
            dest="${VENDOR_DIR}/${p}/config"
            mkdir -p "${dest}"
            if [[ -d config/initdata ]]; then
              rm -rf "${dest}/initdata"
              cp -R config/initdata "${dest}/initdata"
              rm -rf "${dest}/initdata/postgresql" "${dest}/initdata/scripts" \
                "${dest}/initdata/update" "${dest}/initdata/secrets" 2>/dev/null || true
              find "${dest}/initdata" \( -name 'README.md' -o -name 'AGENTS.md' \) -delete 2>/dev/null || true
              rm -rf "${dest}/initdata/pipeline-templates/personal" "${dest}/initdata/pipeline-templates/team"
            fi
            if [[ -d config/profiles/personal/initdata ]]; then
              mkdir -p "${dest}/profiles/personal"
              rm -rf "${dest}/profiles/personal/initdata"
              cp -R config/profiles/personal/initdata "${dest}/profiles/personal/initdata"
            fi
          done

          # Build frontend
          STATIC_DIR="${NPM_DIR}/static"
          mkdir -p "${STATIC_DIR}"
          ( cd web && npm ci && CENTAG_INSTALL_ROOT="$PWD/.." CENTAG_EDITION=personal CENTAG_STATIC_DIR="$(realpath "${STATIC_DIR}")" npm run build )

          # Generate checksums
          : > "${VENDOR_DIR}/checksums.txt"
          for p in $PLATFORMS; do
            goos="${p%-*}"; goarch="${p##*-}"
            ext=""; [[ "$goos" == "windows" ]] && ext=".exe"
            f="${VENDOR_DIR}/${p}/centag-personal${ext}"
            sha="$(sha256sum "$f" | awk '{print $1}')"
            printf '%s  %s/centag-personal%s\n' "$sha" "$p" "$ext" >> "${VENDOR_DIR}/checksums.txt"
          done

          # Publish online version
          echo "==> npm publish centag"
          ( cd "${NPM_DIR}" && npm publish --access public )

          # Publish offline version
          echo "==> npm publish centag-offline"
          OFFLINE_STAGE="$(mktemp -d)"
          cp -R "${NPM_DIR}/bin" "${OFFLINE_STAGE}/bin"
          cp -R "${NPM_DIR}/lib" "${OFFLINE_STAGE}/lib"
          cp -R "${NPM_DIR}/static" "${OFFLINE_STAGE}/static"
          cp "${NPM_DIR}/README.md" "${OFFLINE_STAGE}/README.md"
          cp "${NPM_DIR}/package.offline.json" "${OFFLINE_STAGE}/package.json"
          # Do NOT copy .npmignore — offline variant must include bin/vendor/ and static/
          ( cd "${OFFLINE_STAGE}" && npm publish --access public )
          rm -rf "${OFFLINE_STAGE}"
